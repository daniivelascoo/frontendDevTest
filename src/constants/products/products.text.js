export const PRODUCT_CARD = Object.freeze({

});

export const PRODUCTS_HEADER = Object.freeze({
    BASKET: "Cesta",
});

export const PRODUCTS_PAGE = Object.freeze({

});

export const SEARCH_PRODUCTS_PAGE = Object.freeze({

});

export const PRODUCTS_TOAST_WARNING = Object.freeze({
    
})